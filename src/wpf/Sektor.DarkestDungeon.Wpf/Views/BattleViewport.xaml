<UserControl x:Class="Sektor.DarkestDungeon.Wpf.Views.BattleViewport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#14100C" Offset="0" />
                <GradientStop Color="#241A12" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Room backdrop placeholder: the wide working area; free space at the ends -->
        <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="8,200,8,390"
                Background="#0C0C0C" BorderBrush="#3A2E22" BorderThickness="2" CornerRadius="12" />

        <!-- Player party: four fixed slots on the left half, clear center gap -->
        <ItemsControl ItemsSource="{Binding HeroSlots}"
                      HorizontalAlignment="Left" VerticalAlignment="Center"
                      Margin="40,0,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding}" ContentTemplate="{StaticResource BattleSlotTemplate}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Enemies: four fixed slots on the right half, clear center gap -->
        <ItemsControl ItemsSource="{Binding MonsterSlots}"
                      HorizontalAlignment="Right" VerticalAlignment="Center"
                      Margin="0,0,40,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding}" ContentTemplate="{StaticResource BattleSlotTemplate}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
