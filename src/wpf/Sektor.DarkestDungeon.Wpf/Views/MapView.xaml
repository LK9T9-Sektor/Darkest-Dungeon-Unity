<UserControl x:Class="Sektor.DarkestDungeon.Wpf.Views.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:Sektor.DarkestDungeon.Wpf.Ui">
    <Border Background="#80000000" BorderBrush="#3A2E22" BorderThickness="2" CornerRadius="8" Margin="0,44,0,0">
        <Canvas ClipToBounds="True">
            <!-- Corridor path placeholder -->
            <Path Stroke="#5A4A3A" StrokeThickness="14"
                  Data="M 60,245 C 110,200 140,170 190,175 C 240,180 280,160 340,155 C 400,150 430,180 480,225 C 510,250 560,265 600,272" />

            <!-- Room nodes -->
            <ItemsControl ItemsSource="{Binding Rooms}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="70" Height="54" CornerRadius="10"
                                BorderBrush="{x:Static ui:UiBrushes.Label}" BorderThickness="2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#3A2E22" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsVisited}" Value="True">
                                            <Setter Property="Background" Value="#5A4A3A" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding Type}" Foreground="#E8D9B0" FontSize="14"
                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Current position marker -->
            <Border Width="18" Height="18" CornerRadius="9"
                    Background="{x:Static ui:UiBrushes.Label}"
                    Canvas.Left="46" Canvas.Top="226" />
        </Canvas>
    </Border>
</UserControl>
