<UserControl x:Class="Sektor.DarkestDungeon.Wpf.Views.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Placeholder map: a single centered square; content never overflows the panel. -->
    <Border Style="{StaticResource OverlayPanel}">
        <Grid ClipToBounds="True">
            <Border Width="120" Height="120" CornerRadius="8"
                    BorderBrush="#3A2E22" BorderThickness="2"
                    Background="#140F0C" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="?" Foreground="#8A7A5A" FontSize="30"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
        </Grid>
    </Border>
</UserControl>