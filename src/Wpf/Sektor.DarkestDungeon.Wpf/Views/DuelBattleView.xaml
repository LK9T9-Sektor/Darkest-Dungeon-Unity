<Window x:Class="Sektor.DarkestDungeon.Wpf.Views.DuelBattleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Darkest Dungeon — Duel"
        Width="1024" Height="640"
        Background="#1E1E1E"
        WindowStartupLocation="CenterOwner"
        TextElement.Foreground="White">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Status}" FontSize="18" Foreground="#D9B46A" Margin="0,0,0,8" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ItemsControl Grid.Column="0" ItemsSource="{Binding Heroes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Background="#2B2B2B" Foreground="White" BorderThickness="1" Margin="2" Padding="6"
                                HorizontalContentAlignment="Stretch"
                                Command="{Binding DataContext.TargetCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTarget}" Value="True">
                                            <Setter Property="Background" Value="#3A3A2A" />
                                            <Setter Property="BorderBrush" Value="#D9B46A" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                            <Setter Property="BorderBrush" Value="White" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                                <TextBlock Text="{Binding ClassName}" FontSize="11" Foreground="#AAA" />
                                <TextBlock Text="{Binding HpText}" FontSize="12" />
                                <TextBlock Text="{Binding Stress, StringFormat=Stress {0}}" FontSize="11" Foreground="#777" />
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ItemsControl Grid.Column="1" ItemsSource="{Binding Monsters}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Background="#2B2B2B" Foreground="White" BorderThickness="1" Margin="2" Padding="6"
                                HorizontalContentAlignment="Stretch"
                                Command="{Binding DataContext.TargetCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTarget}" Value="True">
                                            <Setter Property="Background" Value="#3A3A2A" />
                                            <Setter Property="BorderBrush" Value="#D9B46A" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                            <Setter Property="BorderBrush" Value="White" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                                <TextBlock Text="{Binding ClassName}" FontSize="11" Foreground="#AAA" />
                                <TextBlock Text="{Binding HpText}" FontSize="12" />
                                <TextBlock Text="{Binding Stress, StringFormat=Stress {0}}" FontSize="11" Foreground="#777" />
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <TextBlock Grid.Row="2" Text="Skills" FontSize="14" Margin="0,10,0,4" />
        <ItemsControl Grid.Row="3" ItemsSource="{Binding Skills}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding DisplayName}" Width="150" Height="34" Margin="2"
                            Background="#2B2B2B" Foreground="White" BorderBrush="#555"
                            IsEnabled="{Binding IsUsable}"
                            Command="{Binding DataContext.SelectSkillCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Border Grid.Row="3" HorizontalAlignment="Right" Width="340" Height="120" Margin="8,0,0,0"
                Background="#141414" Padding="6">
            <ScrollViewer>
                <ItemsControl ItemsSource="{Binding Log}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="10" Foreground="#888" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>